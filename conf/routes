POST /authenticate   authentication.AuthController.authenticate()

GET /tabs/:tabId tabs.TabsController.getTab(tabId: Long)

GET /tabs/:tabId/bills bills.BillController.getBills(tabId: Long)
POST /tabs/:tabId/bills bills.BillController.postBill(tabId: Long)

GET /tabs tabs.TabsController.getAll()
POST /tabs tabs.TabsController.postTab()

